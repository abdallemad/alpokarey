import { PathsCatalogSkeleton } from "@/components/paths";

/** The route segment's own placeholder — the same one the view uses. */
export default function PathsCatalogLoading() {
  return <PathsCatalogSkeleton />;
}
